/**
 * Expo Router index screen.
 * The actual app navigation is handled by RootNavigator in _layout.tsx.
 * This file exists to satisfy expo-router's file-based routing requirement.
 */

import { View } from 'react-native';

export default function IndexScreen() {
  // RootNavigator in _layout.tsx handles all navigation
  return <View />;
}
